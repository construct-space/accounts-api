# Server configuration
PORT=8000

# App URL (used for OAuth redirects and cookie security)
APP_URL=http://localhost:8000

# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3050,http://localhost:3030,tauri://localhost

# Database
DB_DRIVER=mysql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=construct
DB_USER=root
DB_PASS=

# Added 2026-09-08 from the deployment's keys
DELIVERY_API_KEY=change-me
DELIVERY_URL=https://api.construct.delivery
EMAIL_FROM=Construct <noreply@construct.space>
INTERNAL_SHARED_SECRET=change-me
WEBAUTHN_RPID=construct.space
WEBAUTHN_ORIGINS=https://my.construct.space
SOURCE_URL=http://source
DEVELOPER_URL=http://developer
STORAGE_UPSTREAM=http://storage-api
